services:
  - type: web
    name: circus-app
    runtime: docker
    plan: free
    dockerfilePath: Dockerfile
    healthCheckPath: /health
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: circus_db
          property: connectionString

databases:
  - name: circus_db
    plan: free
