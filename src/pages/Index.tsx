import { CircusGame } from '@/components/game/CircusGame';

const Index = () => {
  return (
    <div className="min-h-screen bg-background flex items-center justify-center">
      <CircusGame />
    </div>
  );
};

export default Index;
